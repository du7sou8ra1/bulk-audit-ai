# BulkAuditAI backend — CORE runtime dependencies only.
#
# The security analyzers (slither, mythril, semgrep, solc-select) are NOT here:
# they are CLI tools invoked as subprocesses, and mythril's pins conflict with
# web3 (eth-hash). Install them in isolated environments with pipx instead —
# see install_tools.sh / README. The app runs fine without them (Tool Health
# just marks them missing and scans record a skipped tool step).
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.1
python-dotenv>=1.0
aiofiles>=23.2
httpx>=0.27
web3>=6.15
eth-utils>=4.0
eth-abi>=4.0
requests>=2.31
pandas>=2.1

# Dev / tests
pytest>=8.0
